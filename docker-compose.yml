version: "3.9"

services:
  db:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_DB: mineralmatrix
      POSTGRES_USER: mineralmatrix
      POSTGRES_PASSWORD: mineralmatrix
    ports:
      - "5432:5432"
